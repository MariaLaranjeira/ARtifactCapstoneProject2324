using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class JumpToNavigation2 : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        SceneHistoryManager.JumpToNavigationPage();
    }
}